// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBanks(t *testing.T) {
	t.Parallel()

	query := Banks()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBanksDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanksQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Banks().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanksSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BankSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanksExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BankExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Bank exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BankExists to return true, but got false.")
	}
}

func testBanksFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	bankFound, err := FindBank(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if bankFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBanksBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Banks().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testBanksOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Banks().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBanksAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	bankOne := &Bank{}
	bankTwo := &Bank{}
	if err = randomize.Struct(seed, bankOne, bankDBTypes, false, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}
	if err = randomize.Struct(seed, bankTwo, bankDBTypes, false, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = bankOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bankTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Banks().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBanksCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	bankOne := &Bank{}
	bankTwo := &Bank{}
	if err = randomize.Struct(seed, bankOne, bankDBTypes, false, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}
	if err = randomize.Struct(seed, bankTwo, bankDBTypes, false, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = bankOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bankTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func bankBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func bankAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Bank) error {
	*o = Bank{}
	return nil
}

func testBanksHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &Bank{}
	o := &Bank{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, bankDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Bank object: %s", err)
	}

	AddBankHook(boil.BeforeInsertHook, bankBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	bankBeforeInsertHooks = []BankHook{}

	AddBankHook(boil.AfterInsertHook, bankAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	bankAfterInsertHooks = []BankHook{}

	AddBankHook(boil.AfterSelectHook, bankAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	bankAfterSelectHooks = []BankHook{}

	AddBankHook(boil.BeforeUpdateHook, bankBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	bankBeforeUpdateHooks = []BankHook{}

	AddBankHook(boil.AfterUpdateHook, bankAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	bankAfterUpdateHooks = []BankHook{}

	AddBankHook(boil.BeforeDeleteHook, bankBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	bankBeforeDeleteHooks = []BankHook{}

	AddBankHook(boil.AfterDeleteHook, bankAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	bankAfterDeleteHooks = []BankHook{}

	AddBankHook(boil.BeforeUpsertHook, bankBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	bankBeforeUpsertHooks = []BankHook{}

	AddBankHook(boil.AfterUpsertHook, bankAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	bankAfterUpsertHooks = []BankHook{}
}

func testBanksInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBanksInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(bankColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBankToManyUserBanks(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Bank
	var b, c UserBank

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, userBankDBTypes, false, userBankColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userBankDBTypes, false, userBankColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.BankID = a.ID
	c.BankID = a.ID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.UserBanks().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.BankID == b.BankID {
			bFound = true
		}
		if v.BankID == c.BankID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := BankSlice{&a}
	if err = a.L.LoadUserBanks(ctx, tx, false, (*[]*Bank)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBanks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.UserBanks = nil
	if err = a.L.LoadUserBanks(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBanks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testBankToManyAddOpUserBanks(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Bank
	var b, c, d, e UserBank

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, bankDBTypes, false, strmangle.SetComplement(bankPrimaryKeyColumns, bankColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*UserBank{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, userBankDBTypes, false, strmangle.SetComplement(userBankPrimaryKeyColumns, userBankColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*UserBank{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddUserBanks(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.BankID {
			t.Error("foreign key was wrong value", a.ID, first.BankID)
		}
		if a.ID != second.BankID {
			t.Error("foreign key was wrong value", a.ID, second.BankID)
		}

		if first.R.Bank != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Bank != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.UserBanks[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.UserBanks[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.UserBanks().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testBanksReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBanksReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BankSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBanksSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Banks().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	bankDBTypes = map[string]string{`ID`: `uuid`, `BankName`: `character varying`, `IsActive`: `boolean`}
	_           = bytes.MinRead
)

func testBanksUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(bankPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(bankAllColumns) == len(bankPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bankDBTypes, true, bankPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBanksSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(bankAllColumns) == len(bankPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Bank{}
	if err = randomize.Struct(seed, o, bankDBTypes, true, bankColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bankDBTypes, true, bankPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(bankAllColumns, bankPrimaryKeyColumns) {
		fields = bankAllColumns
	} else {
		fields = strmangle.SetComplement(
			bankAllColumns,
			bankPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BankSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBanksUpsert(t *testing.T) {
	t.Parallel()

	if len(bankAllColumns) == len(bankPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Bank{}
	if err = randomize.Struct(seed, &o, bankDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Bank: %s", err)
	}

	count, err := Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, bankDBTypes, false, bankPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Bank struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Bank: %s", err)
	}

	count, err = Banks().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
