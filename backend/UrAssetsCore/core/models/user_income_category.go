// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserIncomeCategory is an object representing the database table.
type UserIncomeCategory struct {
	ID     string `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID string `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Name   string `boil:"name" json:"name" toml:"name" yaml:"name"`

	R *userIncomeCategoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userIncomeCategoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserIncomeCategoryColumns = struct {
	ID     string
	UserID string
	Name   string
}{
	ID:     "id",
	UserID: "user_id",
	Name:   "name",
}

var UserIncomeCategoryTableColumns = struct {
	ID     string
	UserID string
	Name   string
}{
	ID:     "user_income_category.id",
	UserID: "user_income_category.user_id",
	Name:   "user_income_category.name",
}

// Generated where

var UserIncomeCategoryWhere = struct {
	ID     whereHelperstring
	UserID whereHelperstring
	Name   whereHelperstring
}{
	ID:     whereHelperstring{field: "\"user_income_category\".\"id\""},
	UserID: whereHelperstring{field: "\"user_income_category\".\"user_id\""},
	Name:   whereHelperstring{field: "\"user_income_category\".\"name\""},
}

// UserIncomeCategoryRels is where relationship names are stored.
var UserIncomeCategoryRels = struct {
	User string
}{
	User: "User",
}

// userIncomeCategoryR is where relationships are stored.
type userIncomeCategoryR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userIncomeCategoryR) NewStruct() *userIncomeCategoryR {
	return &userIncomeCategoryR{}
}

func (r *userIncomeCategoryR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// userIncomeCategoryL is where Load methods for each relationship are stored.
type userIncomeCategoryL struct{}

var (
	userIncomeCategoryAllColumns            = []string{"id", "user_id", "name"}
	userIncomeCategoryColumnsWithoutDefault = []string{"user_id", "name"}
	userIncomeCategoryColumnsWithDefault    = []string{"id"}
	userIncomeCategoryPrimaryKeyColumns     = []string{"id"}
	userIncomeCategoryGeneratedColumns      = []string{}
)

type (
	// UserIncomeCategorySlice is an alias for a slice of pointers to UserIncomeCategory.
	// This should almost always be used instead of []UserIncomeCategory.
	UserIncomeCategorySlice []*UserIncomeCategory
	// UserIncomeCategoryHook is the signature for custom UserIncomeCategory hook methods
	UserIncomeCategoryHook func(context.Context, boil.ContextExecutor, *UserIncomeCategory) error

	userIncomeCategoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userIncomeCategoryType                 = reflect.TypeOf(&UserIncomeCategory{})
	userIncomeCategoryMapping              = queries.MakeStructMapping(userIncomeCategoryType)
	userIncomeCategoryPrimaryKeyMapping, _ = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, userIncomeCategoryPrimaryKeyColumns)
	userIncomeCategoryInsertCacheMut       sync.RWMutex
	userIncomeCategoryInsertCache          = make(map[string]insertCache)
	userIncomeCategoryUpdateCacheMut       sync.RWMutex
	userIncomeCategoryUpdateCache          = make(map[string]updateCache)
	userIncomeCategoryUpsertCacheMut       sync.RWMutex
	userIncomeCategoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userIncomeCategoryAfterSelectMu sync.Mutex
var userIncomeCategoryAfterSelectHooks []UserIncomeCategoryHook

var userIncomeCategoryBeforeInsertMu sync.Mutex
var userIncomeCategoryBeforeInsertHooks []UserIncomeCategoryHook
var userIncomeCategoryAfterInsertMu sync.Mutex
var userIncomeCategoryAfterInsertHooks []UserIncomeCategoryHook

var userIncomeCategoryBeforeUpdateMu sync.Mutex
var userIncomeCategoryBeforeUpdateHooks []UserIncomeCategoryHook
var userIncomeCategoryAfterUpdateMu sync.Mutex
var userIncomeCategoryAfterUpdateHooks []UserIncomeCategoryHook

var userIncomeCategoryBeforeDeleteMu sync.Mutex
var userIncomeCategoryBeforeDeleteHooks []UserIncomeCategoryHook
var userIncomeCategoryAfterDeleteMu sync.Mutex
var userIncomeCategoryAfterDeleteHooks []UserIncomeCategoryHook

var userIncomeCategoryBeforeUpsertMu sync.Mutex
var userIncomeCategoryBeforeUpsertHooks []UserIncomeCategoryHook
var userIncomeCategoryAfterUpsertMu sync.Mutex
var userIncomeCategoryAfterUpsertHooks []UserIncomeCategoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserIncomeCategory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserIncomeCategory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserIncomeCategory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserIncomeCategory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserIncomeCategory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserIncomeCategory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserIncomeCategory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserIncomeCategory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserIncomeCategory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userIncomeCategoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserIncomeCategoryHook registers your hook function for all future operations.
func AddUserIncomeCategoryHook(hookPoint boil.HookPoint, userIncomeCategoryHook UserIncomeCategoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		userIncomeCategoryAfterSelectMu.Lock()
		userIncomeCategoryAfterSelectHooks = append(userIncomeCategoryAfterSelectHooks, userIncomeCategoryHook)
		userIncomeCategoryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		userIncomeCategoryBeforeInsertMu.Lock()
		userIncomeCategoryBeforeInsertHooks = append(userIncomeCategoryBeforeInsertHooks, userIncomeCategoryHook)
		userIncomeCategoryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		userIncomeCategoryAfterInsertMu.Lock()
		userIncomeCategoryAfterInsertHooks = append(userIncomeCategoryAfterInsertHooks, userIncomeCategoryHook)
		userIncomeCategoryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		userIncomeCategoryBeforeUpdateMu.Lock()
		userIncomeCategoryBeforeUpdateHooks = append(userIncomeCategoryBeforeUpdateHooks, userIncomeCategoryHook)
		userIncomeCategoryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		userIncomeCategoryAfterUpdateMu.Lock()
		userIncomeCategoryAfterUpdateHooks = append(userIncomeCategoryAfterUpdateHooks, userIncomeCategoryHook)
		userIncomeCategoryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		userIncomeCategoryBeforeDeleteMu.Lock()
		userIncomeCategoryBeforeDeleteHooks = append(userIncomeCategoryBeforeDeleteHooks, userIncomeCategoryHook)
		userIncomeCategoryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		userIncomeCategoryAfterDeleteMu.Lock()
		userIncomeCategoryAfterDeleteHooks = append(userIncomeCategoryAfterDeleteHooks, userIncomeCategoryHook)
		userIncomeCategoryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		userIncomeCategoryBeforeUpsertMu.Lock()
		userIncomeCategoryBeforeUpsertHooks = append(userIncomeCategoryBeforeUpsertHooks, userIncomeCategoryHook)
		userIncomeCategoryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		userIncomeCategoryAfterUpsertMu.Lock()
		userIncomeCategoryAfterUpsertHooks = append(userIncomeCategoryAfterUpsertHooks, userIncomeCategoryHook)
		userIncomeCategoryAfterUpsertMu.Unlock()
	}
}

// One returns a single userIncomeCategory record from the query.
func (q userIncomeCategoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserIncomeCategory, error) {
	o := &UserIncomeCategory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for user_income_category")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserIncomeCategory records from the query.
func (q userIncomeCategoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserIncomeCategorySlice, error) {
	var o []*UserIncomeCategory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UserIncomeCategory slice")
	}

	if len(userIncomeCategoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserIncomeCategory records in the query.
func (q userIncomeCategoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count user_income_category rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userIncomeCategoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if user_income_category exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserIncomeCategory) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userIncomeCategoryL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserIncomeCategory interface{}, mods queries.Applicator) error {
	var slice []*UserIncomeCategory
	var object *UserIncomeCategory

	if singular {
		var ok bool
		object, ok = maybeUserIncomeCategory.(*UserIncomeCategory)
		if !ok {
			object = new(UserIncomeCategory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserIncomeCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserIncomeCategory))
			}
		}
	} else {
		s, ok := maybeUserIncomeCategory.(*[]*UserIncomeCategory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserIncomeCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserIncomeCategory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &userIncomeCategoryR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userIncomeCategoryR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserIncomeCategories = append(foreign.R.UserIncomeCategories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserIncomeCategories = append(foreign.R.UserIncomeCategories, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the userIncomeCategory to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserIncomeCategories.
func (o *UserIncomeCategory) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_income_category\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userIncomeCategoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userIncomeCategoryR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserIncomeCategories: UserIncomeCategorySlice{o},
		}
	} else {
		related.R.UserIncomeCategories = append(related.R.UserIncomeCategories, o)
	}

	return nil
}

// UserIncomeCategories retrieves all the records using an executor.
func UserIncomeCategories(mods ...qm.QueryMod) userIncomeCategoryQuery {
	mods = append(mods, qm.From("\"user_income_category\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_income_category\".*"})
	}

	return userIncomeCategoryQuery{q}
}

// FindUserIncomeCategory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserIncomeCategory(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*UserIncomeCategory, error) {
	userIncomeCategoryObj := &UserIncomeCategory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_income_category\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userIncomeCategoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from user_income_category")
	}

	if err = userIncomeCategoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userIncomeCategoryObj, err
	}

	return userIncomeCategoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserIncomeCategory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_income_category provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userIncomeCategoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userIncomeCategoryInsertCacheMut.RLock()
	cache, cached := userIncomeCategoryInsertCache[key]
	userIncomeCategoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userIncomeCategoryAllColumns,
			userIncomeCategoryColumnsWithDefault,
			userIncomeCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_income_category\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_income_category\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into user_income_category")
	}

	if !cached {
		userIncomeCategoryInsertCacheMut.Lock()
		userIncomeCategoryInsertCache[key] = cache
		userIncomeCategoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserIncomeCategory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserIncomeCategory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userIncomeCategoryUpdateCacheMut.RLock()
	cache, cached := userIncomeCategoryUpdateCache[key]
	userIncomeCategoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userIncomeCategoryAllColumns,
			userIncomeCategoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update user_income_category, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_income_category\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userIncomeCategoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, append(wl, userIncomeCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update user_income_category row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for user_income_category")
	}

	if !cached {
		userIncomeCategoryUpdateCacheMut.Lock()
		userIncomeCategoryUpdateCache[key] = cache
		userIncomeCategoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userIncomeCategoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for user_income_category")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for user_income_category")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserIncomeCategorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userIncomeCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_income_category\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userIncomeCategoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in userIncomeCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all userIncomeCategory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserIncomeCategory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no user_income_category provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userIncomeCategoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userIncomeCategoryUpsertCacheMut.RLock()
	cache, cached := userIncomeCategoryUpsertCache[key]
	userIncomeCategoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userIncomeCategoryAllColumns,
			userIncomeCategoryColumnsWithDefault,
			userIncomeCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userIncomeCategoryAllColumns,
			userIncomeCategoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert user_income_category, could not build update column list")
		}

		ret := strmangle.SetComplement(userIncomeCategoryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userIncomeCategoryPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert user_income_category, could not build conflict column list")
			}

			conflict = make([]string, len(userIncomeCategoryPrimaryKeyColumns))
			copy(conflict, userIncomeCategoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_income_category\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userIncomeCategoryType, userIncomeCategoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert user_income_category")
	}

	if !cached {
		userIncomeCategoryUpsertCacheMut.Lock()
		userIncomeCategoryUpsertCache[key] = cache
		userIncomeCategoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserIncomeCategory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserIncomeCategory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserIncomeCategory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userIncomeCategoryPrimaryKeyMapping)
	sql := "DELETE FROM \"user_income_category\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from user_income_category")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for user_income_category")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userIncomeCategoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userIncomeCategoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user_income_category")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_income_category")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserIncomeCategorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userIncomeCategoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userIncomeCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_income_category\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userIncomeCategoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from userIncomeCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_income_category")
	}

	if len(userIncomeCategoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserIncomeCategory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserIncomeCategory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserIncomeCategorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserIncomeCategorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userIncomeCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_income_category\".* FROM \"user_income_category\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userIncomeCategoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserIncomeCategorySlice")
	}

	*o = slice

	return nil
}

// UserIncomeCategoryExists checks if the UserIncomeCategory row exists.
func UserIncomeCategoryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_income_category\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if user_income_category exists")
	}

	return exists, nil
}

// Exists checks if the UserIncomeCategory row exists.
func (o *UserIncomeCategory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserIncomeCategoryExists(ctx, exec, o.ID)
}
