// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CurrentMonthIncome is an object representing the database table.
type CurrentMonthIncome struct {
	// Auto Generate UUID V4
	ID          string        `boil:"id" json:"id" toml:"id" yaml:"id"`
	BankUserID  string        `boil:"bank_user_id" json:"bank_user_id" toml:"bank_user_id" yaml:"bank_user_id"`
	UserID      string        `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Amount      types.Decimal `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	PeriodMonth time.Time     `boil:"period_month" json:"period_month" toml:"period_month" yaml:"period_month"`
	IsPayed     null.Bool     `boil:"is_payed" json:"is_payed,omitempty" toml:"is_payed" yaml:"is_payed,omitempty"`
	Name        string        `boil:"name" json:"name" toml:"name" yaml:"name"`

	R *currentMonthIncomeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L currentMonthIncomeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CurrentMonthIncomeColumns = struct {
	ID          string
	BankUserID  string
	UserID      string
	Amount      string
	PeriodMonth string
	IsPayed     string
	Name        string
}{
	ID:          "id",
	BankUserID:  "bank_user_id",
	UserID:      "user_id",
	Amount:      "amount",
	PeriodMonth: "period_month",
	IsPayed:     "is_payed",
	Name:        "name",
}

var CurrentMonthIncomeTableColumns = struct {
	ID          string
	BankUserID  string
	UserID      string
	Amount      string
	PeriodMonth string
	IsPayed     string
	Name        string
}{
	ID:          "current_month_income.id",
	BankUserID:  "current_month_income.bank_user_id",
	UserID:      "current_month_income.user_id",
	Amount:      "current_month_income.amount",
	PeriodMonth: "current_month_income.period_month",
	IsPayed:     "current_month_income.is_payed",
	Name:        "current_month_income.name",
}

// Generated where

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var CurrentMonthIncomeWhere = struct {
	ID          whereHelperstring
	BankUserID  whereHelperstring
	UserID      whereHelperstring
	Amount      whereHelpertypes_Decimal
	PeriodMonth whereHelpertime_Time
	IsPayed     whereHelpernull_Bool
	Name        whereHelperstring
}{
	ID:          whereHelperstring{field: "\"current_month_income\".\"id\""},
	BankUserID:  whereHelperstring{field: "\"current_month_income\".\"bank_user_id\""},
	UserID:      whereHelperstring{field: "\"current_month_income\".\"user_id\""},
	Amount:      whereHelpertypes_Decimal{field: "\"current_month_income\".\"amount\""},
	PeriodMonth: whereHelpertime_Time{field: "\"current_month_income\".\"period_month\""},
	IsPayed:     whereHelpernull_Bool{field: "\"current_month_income\".\"is_payed\""},
	Name:        whereHelperstring{field: "\"current_month_income\".\"name\""},
}

// CurrentMonthIncomeRels is where relationship names are stored.
var CurrentMonthIncomeRels = struct {
	BankUser string
	User     string
}{
	BankUser: "BankUser",
	User:     "User",
}

// currentMonthIncomeR is where relationships are stored.
type currentMonthIncomeR struct {
	BankUser *UserBank `boil:"BankUser" json:"BankUser" toml:"BankUser" yaml:"BankUser"`
	User     *User     `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*currentMonthIncomeR) NewStruct() *currentMonthIncomeR {
	return &currentMonthIncomeR{}
}

func (r *currentMonthIncomeR) GetBankUser() *UserBank {
	if r == nil {
		return nil
	}
	return r.BankUser
}

func (r *currentMonthIncomeR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// currentMonthIncomeL is where Load methods for each relationship are stored.
type currentMonthIncomeL struct{}

var (
	currentMonthIncomeAllColumns            = []string{"id", "bank_user_id", "user_id", "amount", "period_month", "is_payed", "name"}
	currentMonthIncomeColumnsWithoutDefault = []string{"bank_user_id", "user_id", "amount", "period_month", "name"}
	currentMonthIncomeColumnsWithDefault    = []string{"id", "is_payed"}
	currentMonthIncomePrimaryKeyColumns     = []string{"id"}
	currentMonthIncomeGeneratedColumns      = []string{}
)

type (
	// CurrentMonthIncomeSlice is an alias for a slice of pointers to CurrentMonthIncome.
	// This should almost always be used instead of []CurrentMonthIncome.
	CurrentMonthIncomeSlice []*CurrentMonthIncome
	// CurrentMonthIncomeHook is the signature for custom CurrentMonthIncome hook methods
	CurrentMonthIncomeHook func(context.Context, boil.ContextExecutor, *CurrentMonthIncome) error

	currentMonthIncomeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	currentMonthIncomeType                 = reflect.TypeOf(&CurrentMonthIncome{})
	currentMonthIncomeMapping              = queries.MakeStructMapping(currentMonthIncomeType)
	currentMonthIncomePrimaryKeyMapping, _ = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, currentMonthIncomePrimaryKeyColumns)
	currentMonthIncomeInsertCacheMut       sync.RWMutex
	currentMonthIncomeInsertCache          = make(map[string]insertCache)
	currentMonthIncomeUpdateCacheMut       sync.RWMutex
	currentMonthIncomeUpdateCache          = make(map[string]updateCache)
	currentMonthIncomeUpsertCacheMut       sync.RWMutex
	currentMonthIncomeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var currentMonthIncomeAfterSelectMu sync.Mutex
var currentMonthIncomeAfterSelectHooks []CurrentMonthIncomeHook

var currentMonthIncomeBeforeInsertMu sync.Mutex
var currentMonthIncomeBeforeInsertHooks []CurrentMonthIncomeHook
var currentMonthIncomeAfterInsertMu sync.Mutex
var currentMonthIncomeAfterInsertHooks []CurrentMonthIncomeHook

var currentMonthIncomeBeforeUpdateMu sync.Mutex
var currentMonthIncomeBeforeUpdateHooks []CurrentMonthIncomeHook
var currentMonthIncomeAfterUpdateMu sync.Mutex
var currentMonthIncomeAfterUpdateHooks []CurrentMonthIncomeHook

var currentMonthIncomeBeforeDeleteMu sync.Mutex
var currentMonthIncomeBeforeDeleteHooks []CurrentMonthIncomeHook
var currentMonthIncomeAfterDeleteMu sync.Mutex
var currentMonthIncomeAfterDeleteHooks []CurrentMonthIncomeHook

var currentMonthIncomeBeforeUpsertMu sync.Mutex
var currentMonthIncomeBeforeUpsertHooks []CurrentMonthIncomeHook
var currentMonthIncomeAfterUpsertMu sync.Mutex
var currentMonthIncomeAfterUpsertHooks []CurrentMonthIncomeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CurrentMonthIncome) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CurrentMonthIncome) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CurrentMonthIncome) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CurrentMonthIncome) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CurrentMonthIncome) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CurrentMonthIncome) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CurrentMonthIncome) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CurrentMonthIncome) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CurrentMonthIncome) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentMonthIncomeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCurrentMonthIncomeHook registers your hook function for all future operations.
func AddCurrentMonthIncomeHook(hookPoint boil.HookPoint, currentMonthIncomeHook CurrentMonthIncomeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		currentMonthIncomeAfterSelectMu.Lock()
		currentMonthIncomeAfterSelectHooks = append(currentMonthIncomeAfterSelectHooks, currentMonthIncomeHook)
		currentMonthIncomeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		currentMonthIncomeBeforeInsertMu.Lock()
		currentMonthIncomeBeforeInsertHooks = append(currentMonthIncomeBeforeInsertHooks, currentMonthIncomeHook)
		currentMonthIncomeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		currentMonthIncomeAfterInsertMu.Lock()
		currentMonthIncomeAfterInsertHooks = append(currentMonthIncomeAfterInsertHooks, currentMonthIncomeHook)
		currentMonthIncomeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		currentMonthIncomeBeforeUpdateMu.Lock()
		currentMonthIncomeBeforeUpdateHooks = append(currentMonthIncomeBeforeUpdateHooks, currentMonthIncomeHook)
		currentMonthIncomeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		currentMonthIncomeAfterUpdateMu.Lock()
		currentMonthIncomeAfterUpdateHooks = append(currentMonthIncomeAfterUpdateHooks, currentMonthIncomeHook)
		currentMonthIncomeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		currentMonthIncomeBeforeDeleteMu.Lock()
		currentMonthIncomeBeforeDeleteHooks = append(currentMonthIncomeBeforeDeleteHooks, currentMonthIncomeHook)
		currentMonthIncomeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		currentMonthIncomeAfterDeleteMu.Lock()
		currentMonthIncomeAfterDeleteHooks = append(currentMonthIncomeAfterDeleteHooks, currentMonthIncomeHook)
		currentMonthIncomeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		currentMonthIncomeBeforeUpsertMu.Lock()
		currentMonthIncomeBeforeUpsertHooks = append(currentMonthIncomeBeforeUpsertHooks, currentMonthIncomeHook)
		currentMonthIncomeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		currentMonthIncomeAfterUpsertMu.Lock()
		currentMonthIncomeAfterUpsertHooks = append(currentMonthIncomeAfterUpsertHooks, currentMonthIncomeHook)
		currentMonthIncomeAfterUpsertMu.Unlock()
	}
}

// One returns a single currentMonthIncome record from the query.
func (q currentMonthIncomeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CurrentMonthIncome, error) {
	o := &CurrentMonthIncome{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for current_month_income")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CurrentMonthIncome records from the query.
func (q currentMonthIncomeQuery) All(ctx context.Context, exec boil.ContextExecutor) (CurrentMonthIncomeSlice, error) {
	var o []*CurrentMonthIncome

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CurrentMonthIncome slice")
	}

	if len(currentMonthIncomeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CurrentMonthIncome records in the query.
func (q currentMonthIncomeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count current_month_income rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q currentMonthIncomeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if current_month_income exists")
	}

	return count > 0, nil
}

// BankUser pointed to by the foreign key.
func (o *CurrentMonthIncome) BankUser(mods ...qm.QueryMod) userBankQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BankUserID),
	}

	queryMods = append(queryMods, mods...)

	return UserBanks(queryMods...)
}

// User pointed to by the foreign key.
func (o *CurrentMonthIncome) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadBankUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (currentMonthIncomeL) LoadBankUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCurrentMonthIncome interface{}, mods queries.Applicator) error {
	var slice []*CurrentMonthIncome
	var object *CurrentMonthIncome

	if singular {
		var ok bool
		object, ok = maybeCurrentMonthIncome.(*CurrentMonthIncome)
		if !ok {
			object = new(CurrentMonthIncome)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCurrentMonthIncome)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCurrentMonthIncome))
			}
		}
	} else {
		s, ok := maybeCurrentMonthIncome.(*[]*CurrentMonthIncome)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCurrentMonthIncome)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCurrentMonthIncome))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &currentMonthIncomeR{}
		}
		args[object.BankUserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &currentMonthIncomeR{}
			}

			args[obj.BankUserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`user_banks`),
		qm.WhereIn(`user_banks.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UserBank")
	}

	var resultSlice []*UserBank
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UserBank")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user_banks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_banks")
	}

	if len(userBankAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BankUser = foreign
		if foreign.R == nil {
			foreign.R = &userBankR{}
		}
		foreign.R.BankUserCurrentMonthIncomes = append(foreign.R.BankUserCurrentMonthIncomes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BankUserID == foreign.ID {
				local.R.BankUser = foreign
				if foreign.R == nil {
					foreign.R = &userBankR{}
				}
				foreign.R.BankUserCurrentMonthIncomes = append(foreign.R.BankUserCurrentMonthIncomes, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (currentMonthIncomeL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCurrentMonthIncome interface{}, mods queries.Applicator) error {
	var slice []*CurrentMonthIncome
	var object *CurrentMonthIncome

	if singular {
		var ok bool
		object, ok = maybeCurrentMonthIncome.(*CurrentMonthIncome)
		if !ok {
			object = new(CurrentMonthIncome)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCurrentMonthIncome)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCurrentMonthIncome))
			}
		}
	} else {
		s, ok := maybeCurrentMonthIncome.(*[]*CurrentMonthIncome)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCurrentMonthIncome)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCurrentMonthIncome))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &currentMonthIncomeR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &currentMonthIncomeR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CurrentMonthIncomes = append(foreign.R.CurrentMonthIncomes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CurrentMonthIncomes = append(foreign.R.CurrentMonthIncomes, local)
				break
			}
		}
	}

	return nil
}

// SetBankUser of the currentMonthIncome to the related item.
// Sets o.R.BankUser to related.
// Adds o to related.R.BankUserCurrentMonthIncomes.
func (o *CurrentMonthIncome) SetBankUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *UserBank) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"current_month_income\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"bank_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, currentMonthIncomePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BankUserID = related.ID
	if o.R == nil {
		o.R = &currentMonthIncomeR{
			BankUser: related,
		}
	} else {
		o.R.BankUser = related
	}

	if related.R == nil {
		related.R = &userBankR{
			BankUserCurrentMonthIncomes: CurrentMonthIncomeSlice{o},
		}
	} else {
		related.R.BankUserCurrentMonthIncomes = append(related.R.BankUserCurrentMonthIncomes, o)
	}

	return nil
}

// SetUser of the currentMonthIncome to the related item.
// Sets o.R.User to related.
// Adds o to related.R.CurrentMonthIncomes.
func (o *CurrentMonthIncome) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"current_month_income\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, currentMonthIncomePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &currentMonthIncomeR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			CurrentMonthIncomes: CurrentMonthIncomeSlice{o},
		}
	} else {
		related.R.CurrentMonthIncomes = append(related.R.CurrentMonthIncomes, o)
	}

	return nil
}

// CurrentMonthIncomes retrieves all the records using an executor.
func CurrentMonthIncomes(mods ...qm.QueryMod) currentMonthIncomeQuery {
	mods = append(mods, qm.From("\"current_month_income\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"current_month_income\".*"})
	}

	return currentMonthIncomeQuery{q}
}

// FindCurrentMonthIncome retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCurrentMonthIncome(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*CurrentMonthIncome, error) {
	currentMonthIncomeObj := &CurrentMonthIncome{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"current_month_income\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, currentMonthIncomeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from current_month_income")
	}

	if err = currentMonthIncomeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return currentMonthIncomeObj, err
	}

	return currentMonthIncomeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CurrentMonthIncome) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no current_month_income provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(currentMonthIncomeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	currentMonthIncomeInsertCacheMut.RLock()
	cache, cached := currentMonthIncomeInsertCache[key]
	currentMonthIncomeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			currentMonthIncomeAllColumns,
			currentMonthIncomeColumnsWithDefault,
			currentMonthIncomeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"current_month_income\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"current_month_income\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into current_month_income")
	}

	if !cached {
		currentMonthIncomeInsertCacheMut.Lock()
		currentMonthIncomeInsertCache[key] = cache
		currentMonthIncomeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CurrentMonthIncome.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CurrentMonthIncome) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	currentMonthIncomeUpdateCacheMut.RLock()
	cache, cached := currentMonthIncomeUpdateCache[key]
	currentMonthIncomeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			currentMonthIncomeAllColumns,
			currentMonthIncomePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update current_month_income, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"current_month_income\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, currentMonthIncomePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, append(wl, currentMonthIncomePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update current_month_income row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for current_month_income")
	}

	if !cached {
		currentMonthIncomeUpdateCacheMut.Lock()
		currentMonthIncomeUpdateCache[key] = cache
		currentMonthIncomeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q currentMonthIncomeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for current_month_income")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for current_month_income")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CurrentMonthIncomeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentMonthIncomePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"current_month_income\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, currentMonthIncomePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in currentMonthIncome slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all currentMonthIncome")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CurrentMonthIncome) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no current_month_income provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(currentMonthIncomeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	currentMonthIncomeUpsertCacheMut.RLock()
	cache, cached := currentMonthIncomeUpsertCache[key]
	currentMonthIncomeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			currentMonthIncomeAllColumns,
			currentMonthIncomeColumnsWithDefault,
			currentMonthIncomeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			currentMonthIncomeAllColumns,
			currentMonthIncomePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert current_month_income, could not build update column list")
		}

		ret := strmangle.SetComplement(currentMonthIncomeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(currentMonthIncomePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert current_month_income, could not build conflict column list")
			}

			conflict = make([]string, len(currentMonthIncomePrimaryKeyColumns))
			copy(conflict, currentMonthIncomePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"current_month_income\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(currentMonthIncomeType, currentMonthIncomeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert current_month_income")
	}

	if !cached {
		currentMonthIncomeUpsertCacheMut.Lock()
		currentMonthIncomeUpsertCache[key] = cache
		currentMonthIncomeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CurrentMonthIncome record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CurrentMonthIncome) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CurrentMonthIncome provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), currentMonthIncomePrimaryKeyMapping)
	sql := "DELETE FROM \"current_month_income\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from current_month_income")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for current_month_income")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q currentMonthIncomeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no currentMonthIncomeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from current_month_income")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for current_month_income")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CurrentMonthIncomeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(currentMonthIncomeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentMonthIncomePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"current_month_income\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, currentMonthIncomePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from currentMonthIncome slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for current_month_income")
	}

	if len(currentMonthIncomeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CurrentMonthIncome) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCurrentMonthIncome(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CurrentMonthIncomeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CurrentMonthIncomeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentMonthIncomePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"current_month_income\".* FROM \"current_month_income\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, currentMonthIncomePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CurrentMonthIncomeSlice")
	}

	*o = slice

	return nil
}

// CurrentMonthIncomeExists checks if the CurrentMonthIncome row exists.
func CurrentMonthIncomeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"current_month_income\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if current_month_income exists")
	}

	return exists, nil
}

// Exists checks if the CurrentMonthIncome row exists.
func (o *CurrentMonthIncome) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CurrentMonthIncomeExists(ctx, exec, o.ID)
}
